version: "3.9"

services:
  medleaft:
    build: .
    container_name: medleaft_app
    ports:
      - "8501:8501"
    volumes:
      - ./Vectordb:/app/Vectordb
      - ./files:/app/files
    env_file:
      - Rag/.env
    restart: unless-stopped